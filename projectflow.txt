1. Create a git repo and clone it in local.
2. Setup your venv:
   -- python3 -m venv venv (as mine is mac the commands are different)
   -- source venv/bin/activate
   -- pip install -r requirements.txt
3. Add .gitignore
4. Create a main backend/ 
                   -- add btw_handler.py
                   -- add models.py
                   -- add paper_loader.py
                   -- add rag_graph.py
                   -- add vector_store.py
                   -- add __init__.py
5. Added about_project.md file (which i designed to have in the project)
6. Started building the backend files:
    1. document loader 
    2. vector_store along with added .env file :
       QDRANT_URL=...
       QDRANT_API_KEY=...
       HUGGINGFACEHUB_API_TOKEN=...
    3. models.py file and btw_handler
       In btw_handler i used the  qwen2.5:3b from ollama as it is light weight task inorder to not under go rate limit.
      The project supports local inference for development and 
      can be configured to use managed cloud models in production 
      without changing the application logic.(Pluggable LLM Architecture)


                      .env
                   │
                   ▼
      ┌───────────────────────┐
      │                       │
      ▼                       ▼
llm_factory.py        embedding_factory.py
      │                       │
      ▼                       ▼
 graph.py              vector_store.py
      │                       │
      ▼                       ▼
 btw_handler.py          CacheBackedEmbeddings
      │                       │
      └───────────┬───────────┘
                  ▼
             LangGraph
                  │
                  ▼
             Streamlit
   -> Also added the stateless memory in the chat history .
   
    
