1. Create a git repo and clone it in local.
2. Setup your venv:
   -- python3 -m venv venv (as mine is mac the commands are different)
   -- source venv/bin/activate
   -- pip install -r requirements.txt
3. Add .gitignore
4. Create a main backend/ 
                   -- add btw_handler.py
                   -- add models.py
                   -- add paper_loader.py
                   -- add rag_graph.py
                   -- add vector_store.py
                   -- add __init__.py
5. Added about_project.md file (which i designed to have in the project)
6. 
